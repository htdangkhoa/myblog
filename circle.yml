version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.0-browsers
    steps:
      - checkout
      - run:
          name: "Deploy to Now"
          command: npm install
        # now -t qlCKyiruBgEDfJ2Ir2zCvpjV